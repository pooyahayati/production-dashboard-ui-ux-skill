# RTL Table Fixture

Purpose: test Persian RTL, mixed-direction values, table UX, localization, and mobile behavior.

Intentional issues:

- hard-coded physical CSS directions
- email/version/ID are not isolated LTR
- dates/currency are plain strings
- table has weak mobile behavior
- no saved-view/column preference model
