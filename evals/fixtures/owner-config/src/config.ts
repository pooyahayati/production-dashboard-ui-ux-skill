export type DesignConfig = {
  primary: string;
  customCss: string;
  density: string;
  sidebarWidth: number;
};

export const activeConfig: DesignConfig = {
  primary: "#4f46e5",
  customCss: "",
  density: "balanced",
  sidebarWidth: 280,
};
